/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Flight-finder-',
  images: { unoptimized: true },
  trailingSlash: true,
};

export default nextConfig;
